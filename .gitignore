# Local environment and editor files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Build outputs and native artifacts
**/build/
**/CMakeFiles/
**/*.o
**/*.obj
**/*.so
**/*.dll
**/*.dylib
**/*.a
**/*.lib
**/*.exe
**/*.pdb
**/*.ilk

# Frontend dependencies and standalone build output
web/node_modules/
web/dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Python caches and virtual environments
__pycache__/
*.py[cod]
.venv/
venv/

# Runtime and benchmark output
*.log
sample/bench_result.txt
sample/ok.txt

# Local secrets and environment overrides
.env
.env.*
!.env.example

# Commercial/reference documents kept outside the source repository
商业文件/
temp/

# Local database/server data
pgdata/
postgres-data/
docker/volumes/

# Upstream source trees copied locally for reference/build experiments.
# maplibre-tile-spec is intentionally not ignored because its C++ encoder
# is a build dependency of boundlessgis.
postgresql/
postgis/
